
IF 5==5 THEN PRINT("if statement example")
ELSE PRINT("code not working")
BLOCK

IF 5!=5 THEN PRINT("code not working")
ELIF 4==4 THEN PRINT("elif statement example")
ELSE PRINT("code not working")
BLOCK

IF 5>=6 THEN PRINT("code not working")
ELSE PRINT("else statement example")
BLOCK

VAR x=10
VAR y=20.05
IF x>y THEN PRINT("x is bigger") ELSE PRINT("y is bigger")
BLOCK

VAR i=0
BLOCK
WHILE i<10 THEN VAR i=i+1
BLOCK

VAR j = 0
BLOCK
VAR result = 0
BLOCK
FOR j = 0 TO 9 THEN VAR result = result + 5
BLOCK
FOR j = 10 TO 0 STEP -2 THEN VAR result = result / 2
BLOCK

#TODO: nested loops

TRUE
BLOCK
FALSE
BLOCK
NULL
BLOCK

(3*(4+4)-6)/2 ; 8^2-(-3)
BLOCK

TRUE AND FALSE;FALSE AND FALSE;TRUE AND TRUE;TRUE OR FALSE;TRUE OR TRUE;FALSE OR FALSE;NOT FALSE;NOT TRUE
BLOCK

VAR string1 = "Hello "
VAR string2 = "World"
VAR string3 = string1+string2
VAR string4 = "Repeat "
VAR string5 = string4 * 8
BLOCK

VAR list1 = [1,2,3]
BLOCK
VAR list2 = [4,5,6]
BLOCK
VAR list3 = list1*list2
BLOCK
VAR list3 = list3 + 7
BLOCK
VAR list3 = list3 - 1
BLOCK
VAR list3 = list3 - -2
BLOCK
VAR list2 = list2 / 1
BLOCK

MATH_PI
IS_NUM(9)
#TODO: IS_NUM($), IS_LIST not working
IS_STR("hey")
IS_STR(5)
BLOCK

VAR list = [0,1,2,3]
BLOCK
APPEND(list,4)
BLOCK
list
BLOCK
POP(list,1)
BLOCK
list
BLOCK
EXTEND(list,[8,7,10])
BLOCK
list
BLOCK

VAR returned = PRINT_RETURN("hey")
BLOCK
returned
BLOCK

FUNC add(num1,num2) ~ num1*num2
#TODO: use the function.
#TODO: implementing INPUT/INPUT_INT, not working in multiline.